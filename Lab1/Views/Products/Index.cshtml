@model IEnumerable<Lab1.Models.Product>

@{
    ViewBag.Title = "Products";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Products</h2>

<ul>
    @foreach (var p in Model)
    {
        <li>@Html.ActionLink(p.Name, "Details", "Products", new { name = p.Name }, null)</li>
    }
</ul>